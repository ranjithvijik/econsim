name: Build & Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Inject API Keys
        run: |
          sed -i "s/__AV_API_KEY__/${{ secrets.AV_API_KEY }}/g" index.html
          sed -i "s/__FRED_API_KEY__/${{ secrets.FRED_API }}/g" index.html
          echo "API keys injected into index.html"

      - name: Verify injection
        run: |
          if grep -q "__AV_API_KEY__" index.html || grep -q "__FRED_API_KEY__" index.html; then
            echo "ERROR: Placeholders were not replaced. Check that AV_API_KEY and FRED_API secrets are set."
            exit 1
          else
            echo "Placeholder successfully replaced."
          fi

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: site
          path: index.html
